import React from "react";

// Placeholder component for ConnectAutomationServerModal
export const ConnectAutomationServerModal: React.FC<{ children: React.ReactNode; apiUrl: string }> = ({ children }) => {
  return <>{children}</>;
};